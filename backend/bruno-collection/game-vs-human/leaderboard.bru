meta {
  name: leaderboard
  type: http
  seq: 9
}

get {
  url: http://localhost:3000/api/games-vs-human/leaderboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "login": "meow",
    "password": "meowmeow"
  }
}

script:post-response {
  const games = res.getBody();
  if (games && games.length > 0) {
    bru.setVar("gameId", games[0].id);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
