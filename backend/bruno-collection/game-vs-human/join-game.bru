meta {
  name: join-game
  type: http
  seq: 3
}

post {
  url: http://localhost:3000/api/games-vs-human/{{gameId}}/join
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken2}}
}

body:json {
  {
    "login": "meow",
    "password": "meowmeow"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
